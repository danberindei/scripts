#!/bin/sh
# Block commits that include a Copilot co-author trailer
if grep -qi 'Co-authored-by:.*Copilot' "$1"; then
  echo "ERROR: Commits co-authored by Copilot are blocked." >&2
  exit 1
fi
exit 0
